#ifndef MOCK_VIEW_HPP
#define MOCK_VIEW_HPP
/*
#include <string>
#include <vector>
#include "gmock/gmock.h"
#include "Hand.hpp"
#include "Player.hpp"
#include "View.hpp"

class MockUI : public UI {
 public:
    MOCK_METHOD(std::string, showHand, (Hand*));
    MOCK_METHOD(std::string, showActions, ());
    MOCK_METHOD(std::string, showWinners, (std::vector<Player*>));
    MOCK_METHOD(std::string, showTurn, (Player*));
    MOCK_METHOD(std::string, showDealt, (Player*));
//do the private methods need mocking?
};

#endif // VIEW_HPP
*/
