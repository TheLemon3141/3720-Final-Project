#ifndef MOCK_HAND_H_INCLUDED
#define MOCK_HAND_H_INCLUDED
/*
#include <vector>
#include "Card.hpp"

class MockHand : public Hand {
 public:
    MOCK_METHOD(void, addCard, (Card*));
    MOCK_METHOD(int, getTotal, ()); //const
    MOCK_METHOD(void, clear, ());
    MOCK_METHOD(const std::vector<Card*>&, getCards, ()); //const;

 private:
    std::vector<Card*> cards;
};

#endif // MOCK_HAND_H_INCLUDED
*/
